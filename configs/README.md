# Configs

The assignment-specific configuration lives here.

The runner converts these files into MaxText command-line overrides. This keeps the submission auditable without modifying MaxText source code.

