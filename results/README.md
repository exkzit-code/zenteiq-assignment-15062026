# Results

Keep raw and derived outputs separate.

- `raw_logs/`: unedited MaxText stdout/stderr logs.
- `metrics/`: CSV and Markdown tables derived from raw logs.
- `screenshots/`: optional Colab screenshots with clear captions.

Do not delete failed-run logs. Failed attempts are useful evidence when explaining hardware limits or configuration changes.

