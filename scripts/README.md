# Scripts

Small helper scripts for:

- estimating parameter counts before running
- launching MaxText runs in Colab
- parsing raw MaxText logs into metric tables

The scripts intentionally avoid hiding MaxText behavior. Raw logs remain the source of truth.

